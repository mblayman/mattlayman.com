This repository is the source for https://www.mattlayman.com.

# Prism config

* Languages selected on builder page are all embedded in the link.
  The link is in the header of the JS file.
* The CSS files are separate because the Automad site doesn't use `prefers-color-scheme` for theme selection.
  The links for those are also at the top of the file.
